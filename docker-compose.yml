services:
  kuzu-explorer:
    image: kuzudb/explorer:latest
    ports:
      - "8000:8000"
    volumes:
      - ./.code-explorer:/database
    environment:
      - KUZU_FILE=graph.db
      - MODE=READ_ONLY
    restart: unless-stopped
